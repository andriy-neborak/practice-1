# 🎮 STM32 Match-3 Hardware Engine — Project Specifications

## 💻 System Requirements (PC Client)

### Operating System:
* [x] Windows 10 / Windows 11 (64-bit)

### Architecture:
* [x] x64 (recommended)

### Minimum Hardware Requirements:
* [x] CPU: Dual-core 1.8 GHz or higher
* [x] RAM: 4 GB
* [x] Storage: ~100 MB free space

## 🛠 Development Environment
**PC Client:**
* [x] Visual Studio Code / PyCharm
* [x] Python Version: 3.11+ *(Рекомендовано 3.12)*
* [x] Graphics: `pygame-ce` (Community Edition)
* [x] Serial Comms: `pyserial` 3.5+

**Hardware Server:**
* [x] **Target Board:** STM32F0Discovery (або аналогічна, напр. STM32F103)
* [x] **MCU:** STM32F051R8 (ARM Cortex-M0) / STM32F103C8T6
* [x] **IDE:** STM32CubeIDE
* [x] **Compiler:** GCC for ARM Embedded Processors (`arm-none-eabi`)

---

## ⚙️ Технічна Архітектура
* **Шаблон:** Клієнт-Сервер (ПК — "Режисер/Монітор", STM32 — "Фізичний рушій").
* **Апаратна логіка:** Всі прорахунки збігів (Match-3), гравітації, генерації поля та перевірки на глухий кут (Deadlock) виконуються на STM32.
* **Анімації:** Реалізовані асинхронно на стороні клієнта (Python) для забезпечення плавних 60 FPS без блокування мікроконтролера.

---

## 📡 Протокол обміну (Binary UART Protocol)

Зв'язок здійснюється через UART (BaudRate: 38400, 8N1).
Обмін даними відбувається бінарними пакетами фіксованої довжини — **6 байт**.
Між пакетами не потрібні роздільники, але рекомендується таймаут прийому **10 мс** для захисту від розсинхронізації.

### 📦 Структура пакету
| Byte 0 | Byte 1 | Byte 2 | Byte 3 | Byte 4 | Byte 5 |
| :---: | :---: | :---: | :---: | :---: | :---: |
| **CMD** | **ADDR_H** | **ADDR_L** | **DATA_H** | **DATA_L** | **CRC-8** |
| Команда | Рядок 1 / Дані | Стовпчик 1 / Дані | Рядок 2 / Колір / Дані | Стовпчик 2 / Статус / Дані | Checksum |

### 🔐 Валідація та CRC-8
Контрольна сума розраховується за повноцінним алгоритмом **CRC-8** (не модульна сума!).
* **Поліном:** `0x07`
* **Init:** `0x00`
* Якщо CRC від клієнта не збігається з розрахованим на платі, STM32 ігнорує команду і повертає діагностичний пакет помилки: `EE [Calc_CRC] [RX_CRC] EE EE [CRC]`.

---

### 📋 Таблиця команд (Commands Reference)

| HEX | Команда | Напрямок | Опис дії та формат даних |
| :---: | :--- | :---: | :--- |
| **`0x10`** | `NEW GAME` | `PC -> MCU` | Ініціалізує нове поле без готових збігів. Обнуляє рахунок.<br>**Відповідь:** Підтвердження `[10 00 00 00 AA CRC]` + автоматичний дамп всього поля через пакети `0x16`. |
| **`0x11`** | `SWAP` | `PC -> MCU` | Запит на хід гравця. Байти 1-4 містять координати: `r1, c1, r2, c2`.<br>**Відповідь (Byte 4):**<br>`AA` — Успіх (хід валідний, запускається каскад).<br>`EE` — Помилка (немає лінії 3-в-ряд, хід відхилено).<br>`DD` — Deadlock (хід валідний, але після падіння кубиків ходів більше немає).<br>*Примітка: У разі успіху MCU автоматично розішле оновлені клітинки (Diff) через `0x16`.* |
| **`0x14`** | `GET CELL` | `PC -> MCU` | Запит кольору конкретної клітинки. Байти 1-2 містять `r, c`.<br>**Відповідь:** У Байті 3 повертається ID кольору (1-6). Байт 4 містить статус `AA` або `EE` (якщо координати хибні). |
| **`0x15`** | `GET SCORE` | `PC -> MCU` | Запит поточного рахунку. <br>**Відповідь:** Рахунок (`uint32_t`) розбивається на 4 байти і передається у Байтах 1, 2, 3, 4 (від найстаршого до наймолодшого). Статусу `AA` у цьому пакеті немає. |
| **`0x16`** | `UPDATE CELL`| `MCU -> PC` | **Асинхронна команда сервера!** Плата сама надсилає цей пакет, якщо клітинка змінила колір після ходу або падіння. Байти 1-2: `r, c`. Байт 3: Новий колір. Байт 4: `AA`. |

### Коди статусів гри (Byte 4)
* **`0xAA`** — OK (Операція успішна).
* **`0xEE`** — ERROR (Помилка логіки / відхилено / невірний CRC).
* **`0xDD`** — DEADLOCK (Гра завершена: немає доступних ходів).
* **`0xFF`** — Невідома команда.
# Гра "Збери лінію" (STM32 MCU Edition)

## 📌 Опис проєкту
Цей проєкт — мікроконтролерна реалізація логічної гри (за типом "Match-4") для плати серії STM32F0. Логіка гри, обробка команд та збереження прогресу виконуються безпосередньо на "залізі" (MCU), а зв'язок з комп'ютером відбувається через UART-протокол.

## ⚙️ Механіка та правила гри
* [cite_start]**Розмір поля:** Стандартний розмір поля становить 8 на 8 клітинок[cite: 3].
* [cite_start]**Кількість кульок:** В звичайному режимі повинно розміщуватись 64 кульки[cite: 13].
* [cite_start]**Кольори:** У грі використовується 6 кольорів: Червоний, Синій, Зелений, Жовтий, Фіолетовий, Помаранчевий[cite: 14, 15, 16, 17, 18, 19, 20].

### Генерація поля
[cite_start]Поява об'єктів на новому полі має відбуватися випадково[cite: 5]. [cite_start]Автоматична генерація готових рядів або стовпців заборонена[cite: 5]. [cite_start]При генерації поля виконується математична перевірка[cite: 6]:

$A_{xy(1)} = A_{xy(2)} = A_{xy(3)} = x$

[cite_start]Де $x$ — це колір нової кульки [cite: 7][cite_start], а $A$ — колір попередньої[cite: 8]. [cite_start]При дійсності цієї перевірки повинен бути обраний новий випадковий колір[cite: 9].

### Нарахування балів
[cite_start]Нарахування відбувається за спалювання ліній однакових кульок[cite: 22]. [cite_start]Мінімальна згоряєма сума кульок — 4[cite: 22]. [cite_start]Бали нараховуються за такою таблицею[cite: 23]:
* [cite_start]**4 кульки:** 100 балів [cite: 23]
* [cite_start]**5 кульок:** 250 балів [cite: 23]
* [cite_start]**6 кульок:** 500 балів [cite: 23]
* [cite_start]**7 кульок:** 1000 балів [cite: 23]
* [cite_start]**8 кульок:** 5000 балів [cite: 23]

*Примітка: У цій версії гри відсутні підказки для гравця, складність базується виключно на формі поля та логіці падіння.*

## 💾 Реалізований функціонал (Енергонезалежна пам'ять)
Проєкт використовує Flash-пам'ять мікроконтролера (останні сторінки) для збереження ігрового прогресу без використання зовнішніх SD-карт.
* **MCU: Save game (#32):** Збереження імені гравця, поточного рахунку та розташування кульок на полі у Flash-пам'ять.
* **MCU: Restore saved game (#33):** Автоматичне завантаження останньої збереженої сесії (Quick Save) після перезавантаження мікроконтролера.
* **MCU: Save users top score (#31):** Запис найкращого результату (Top Score) в окремий слот пам'яті.

## 🚀 Як скомпілювати та прошити проєкт

1. **Відкрийте проєкт:** Запустіть **STM32CubeIDE** та імпортуйте папку з проєктом у свій Workspace.
2. **Перевірте архітектуру:** Переконайтеся, що модулі розділені правильно (`main.c` для UART/HAL, `game.c` для логіки гри, `save.c` для роботи з Flash-пам'яттю).
3. **Компіляція (Build):** Натисніть іконку **молотка (Build)** на верхній панелі інструментів. Дочекайтеся повідомлення `0 errors`.
4. **Підключення:** Підключіть плату STM32F0 Discovery до комп'ютера через USB-кабель ST-LINK.
5. **Прошивка (Flash):** Натисніть кнопку **Run** (зелений трикутник). IDE автоматично зітре стару пам'ять і запише вашу скомпільовану програму.

## 📡 Протокол зв'язку (UART)
Спілкування з платою відбувається пакетами по 6 байт з перевіркою контрольної суми (CRC8).
* `0x10` — Ініціалізація нової гри.
* `0x11` — Хід гравця (SWAP).
* `0x12` — Зберегти гру у Flash-пам'ять.
* `0x13` — Завантажити гру з Flash-пам'яті.
* `0x14` / `0x15` — Запит кольору клітинки / поточного рахунку.
* `0x16` — Асинхронне оновлення клітинки (Плата -> ПК).
