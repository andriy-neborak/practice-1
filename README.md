# 🎮 Гра "Збери лінію" (STM32 Hardware Engine)

## 📌 Опис проєкту
Цей проєкт — мікроконтролерна реалізація логічної гри (за типом "Match-4") для плати серії STM32F0. Логіка гри, обробка команд та збереження прогресу виконуються безпосередньо на апаратному рівні (MCU), а зв'язок з комп'ютером (клієнтом) відбувається через UART-протокол.

---

## 💻 Вимоги до системи та середовище розробки

### PC Client (Клієнт)
* **ОС:** Windows 10 / Windows 11 (64-bit, x64 рекомендовано).
* **Мінімальне залізо:** CPU Dual-core 1.8 GHz+, 4 GB RAM, ~100 MB вільного місця.
* **Середовище:** Visual Studio Code / PyCharm.
* **Стек:** Python 3.11+ (рекомендовано 3.12), `pygame-ce` (графіка), `pyserial` 3.5+ (зв'язок).

### Hardware Server (Мікроконтролер)
* **Плата:** STM32F0Discovery (або аналогічна, напр. STM32F103).
* **Мікроконтролер:** STM32F051R8 (ARM Cortex-M0) / STM32F103C8T6.
* **IDE:** STM32CubeIDE.
* **Компілятор:** GCC for ARM Embedded Processors (`arm-none-eabi`).

---

## ⚙️ Технічна Архітектура
* **Шаблон:** Клієнт-Сервер (ПК — "Режисер/Монітор", STM32 — "Фізичний рушій").
* **Апаратна логіка:** Всі прорахунки збігів, гравітації, генерації поля та перевірки на глухий кут (Deadlock) виконуються на STM32.
* **Анімації:** Реалізовані асинхронно на стороні клієнта (Python) для забезпечення плавних 60 FPS без блокування мікроконтролера.

---

## 🧩 Механіка та правила гри
* [cite_start]**Розмір поля:** Стандартний розмір поля становить 8 на 8 клітинок[cite: 3].
* [cite_start]**Кількість кульок:** В звичайному режимі повинно розміщуватись 64 кульки[cite: 13].
* [cite_start]**Кольори:** У грі використовується 6 кольорів: Червоний, Синій, Зелений, Жовтий, Фіолетовий, Помаранчевий[cite: 14, 15, 16, 17, 18, 19, 20].

### Генерація поля
[cite_start]Поява об'єктів на новому полі має відбуватися випадково[cite: 5]. [cite_start]Автоматична генерація готових рядів або стовпців заборонена[cite: 5]. [cite_start]При генерації поля виконується математична перевірка[cite: 6]:

$A_{xy(1)} = A_{xy(2)} = A_{xy(3)} = x$

[cite_start]Де $x$ — це колір нової кульки [cite: 7][cite_start], а $A$ — колір попередньої[cite: 8]. [cite_start]При дійсності цієї перевірки повинен бути обраний новий випадковий колір[cite: 9].

### Нарахування балів
[cite_start]Нарахування відбувається за спалювання ліній однакових кульок[cite: 22]. [cite_start]Мінімальна згоряєма сума кульок — 4[cite: 22]. [cite_start]Бали нараховуються за такою таблицею[cite: 23]:
* [cite_start]**4 кульки:** 100 балів [cite: 23]
* [cite_start]**5 кульок:** 250 балів [cite: 23]
* [cite_start]**6 кульок:** 500 балів [cite: 23]
* [cite_start]**7 кульок:** 1000 балів [cite: 23]
* [cite_start]**8 кульок:** 5000 балів [cite: 23]

*Примітка: У цій версії гри відсутні підказки для гравця, складність базується виключно на формі поля та логіці падіння.*

---

## 💾 Реалізований функціонал (Енергонезалежна пам'ять - NVM)
Проєкт використовує Flash-пам'ять мікроконтролера для збереження ігрового прогресу без зовнішніх SD-карт.
* **MCU: Save game (#32):** Збереження імені гравця, поточного рахунку та розташування кульок на полі у Flash-пам'ять.
* **MCU: Restore saved game (#33):** Автоматичне завантаження останньої збереженої сесії (Quick Save) після перезавантаження плати.
* **MCU: Save users top score (#31):** Запис найкращого результату (Top Score) в окремий слот пам'яті.

---

## 📡 Протокол обміну (Binary UART Protocol)
Зв'язок здійснюється через UART (BaudRate: 38400, 8N1). Обмін даними відбувається бінарними пакетами фіксованої довжини — **6 байт**. Таймаут прийому — **10 мс**.

### 📦 Структура пакету
| Byte 0 | Byte 1 | Byte 2 | Byte 3 | Byte 4 | Byte 5 |
| :---: | :---: | :---: | :---: | :---: | :---: |
| **CMD** | **ADDR_H** | **ADDR_L** | **DATA_H** | **DATA_L** | **CRC-8** |
| Команда | Рядок 1 / Дані | Стовпчик 1 / Дані | Рядок 2 / Колір / Дані | Стовпчик 2 / Статус / Дані | Checksum |

### 🔐 Валідація та CRC-8
Контрольна сума розраховується за алгоритмом **CRC-8** (Поліном: `0x07`, Init: `0x00`).
Якщо CRC від клієнта не збігається, STM32 ігнорує команду і повертає діагностичний пакет: `EE [Calc_CRC] [RX_CRC] EE EE [CRC]`.

### 📋 Таблиця команд
| HEX | Команда | Напрямок | Опис дії та формат даних |
| :---: | :--- | :---: | :--- |
| **`0x10`** | `NEW GAME` | `PC -> MCU` | Ініціалізує нове поле без готових збігів. Обнуляє рахунок.<br>**Відповідь:** `[10 00 00 00 AA CRC]` + автоматичний дамп всього поля через пакети `0x16`. |
| **`0x11`** | `SWAP` | `PC -> MCU` | Запит на хід гравця. Байти 1-4 містять координати: `r1, c1, r2, c2`.<br>**Відповідь (Byte 4):**<br>`AA` — Успіх (запускається каскад).<br>`EE` — Помилка (немає лінії 3-в-ряд).<br>`DD` — Deadlock (ходів більше немає).<br>*Примітка: У разі успіху MCU розішле оновлені клітинки через `0x16`.* |
| **`0x12`** | `SAVE GAME` | `PC -> MCU` | Зберегти гру у Flash-пам'ять.<br>**Відповідь:** `[12 00 00 00 AA CRC]` |
| **`0x13`** | `LOAD GAME` | `PC -> MCU` | Завантажити гру з Flash-пам'яті.<br>**Відповідь:** `[13 00 00 00 AA CRC]` + дамп поля. |
| **`0x14`** | `GET CELL` | `PC -> MCU` | Запит кольору конкретної клітинки. Байти 1-2 містять `r, c`.<br>**Відповідь:** У Байті 3 повертається ID кольору. Байт 4 містить статус `AA` або `EE`. |
| **`0x15`** | `GET SCORE` | `PC -> MCU` | Запит поточного рахунку.<br>**Відповідь:** Рахунок (`uint32_t`) розбивається на 4 байти і передається у Байтах 1, 2, 3, 4. |
| **`0x16`** | `UPDATE CELL`| `MCU -> PC` | **Асинхронна команда сервера!** Плата сама надсилає цей пакет при зміні клітинки. Байти 1-2: `r, c`. Байт 3: Новий колір. Байт 4: `AA`. |

---

## 🚀 Як скомпілювати та прошити проєкт
1. **Відкрийте проєкт:** Запустіть **STM32CubeIDE** та імпортуйте папку з проєктом.
2. **Перевірте архітектуру:** Переконайтеся, що модулі розділені правильно (`main.c` для UART/HAL, `game.c` для логіки гри, `save.c` для роботи з Flash).
3. **Компіляція (Build):** Натисніть іконку **молотка (Build)**. Дочекайтеся повідомлення `0 errors`.
4. **Підключення:** Підключіть плату STM32F0 Discovery до комп'ютера через USB-кабель ST-LINK.
5. **Прошивка (Flash):** Натисніть кнопку **Run** (зелений трикутник). IDE автоматично зітре стару пам'ять і запише програму.
